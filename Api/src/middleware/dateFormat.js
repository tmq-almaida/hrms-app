export const date_format = new Intl.DateTimeFormat("en-us", {
	dateStyle: "short"
});

export const date_html = new Intl.DateTimeFormat("fr-CA", {
	dateStyle: "short"
});

export const datetime_format = new Intl.DateTimeFormat("en-us", {
	dateStyle: "full",
	timeStyle: "medium"
});
